CREATE TABLE "city" ("city_id" INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, "city" VARCHAR(50) NOT NULL, "country_id" SMALLINT NOT NULL, "last_update" TIMESTAMP WITHOUT TIME ZONE DEFAULT NOW() NOT NULL, CONSTRAINT "city_pkey" PRIMARY KEY ("city_id"));
