CREATE TABLE "actor" ("actor_id" INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, "first_name" VARCHAR(45) NOT NULL, "last_name" VARCHAR(45) NOT NULL, "last_update" TIMESTAMP WITHOUT TIME ZONE DEFAULT NOW() NOT NULL, CONSTRAINT "actor_pkey" PRIMARY KEY ("actor_id"));
